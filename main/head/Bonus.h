#pragma once

#include "Map.h"
#include <vector>
#include <cstdlib>

void setBomb(Map map) {

    /*int width = map.getBinMap().getSize().x;
    int height = map.getBinMap().getSize().y;
    
    for (int indexY = 0; indexY < _size.x; ++indexY) {
        for (int indexX = 0, counter = 0; indexX < _size.y * 2; indexX += 2, ++counter) {}
    }*/
}